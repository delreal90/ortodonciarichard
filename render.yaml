services:
  - type: web
    name: ortodonciarichard-backend
    runtime: python
    rootDir: admin
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: DENTIDESK_EMAIL
        sync: false
      - key: DENTIDESK_PASSWORD
        sync: false
      - key: DENTIDESK_BASIC_USER
        sync: false
      - key: DENTIDESK_BASIC_PASS
        sync: false
